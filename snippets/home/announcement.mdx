export const Announcement = ({ 
  emoji = "🎉",
  title = "Announcement",
  message = "50% off annual plans for life! First 100 founders only",
  href = "https://app.eggz.ai/#pricing",
  bgColor = "#F7F7F6"
}) => {
  const [dismissed, setDismissed] = React.useState(false);
  
  if (dismissed) return null;
  
  return (
    <div 
      className="relative overflow-hidden transition-all duration-300"
      style={{ backgroundColor: bgColor }}
    >
      <a
        href={href}
        className="mx-auto flex w-full flex-col text-center items-center justify-center gap-1 px-10 py-2 text-sm text-zinc-900 hover:underline sm:flex-row"
      >
        <span className="font-semibold">{emoji} {title} {emoji}</span>
        <span>{message}</span>
      </a>
      <button
        onClick={() => setDismissed(true)}
        className="absolute right-3 top-1/2 -translate-y-1/2 text-zinc-900 hover:opacity-70 transition-opacity p-1"
        aria-label="Dismiss announcement"
      >
        <svg 
          xmlns="http://www.w3.org/2000/svg" 
          width="18" 
          height="18" 
          viewBox="0 0 24 24" 
          fill="none" 
          stroke="currentColor" 
          strokeWidth="2" 
          strokeLinecap="round" 
          strokeLinejoin="round"
        >
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
    </div>
  );
};

