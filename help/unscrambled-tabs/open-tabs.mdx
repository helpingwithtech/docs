---
title: "Open Tabs view"
description: "See every tab in every Chrome window in one tidy list, then jump, rename, suspend, save, and close duplicates from one place"
icon: "table-columns"
keywords: ['open tabs', 'all tabs', 'save window', 'suspend others', 'close duplicates']
---

You know the tab is open. You just cannot find which of your four windows it is in.

The **Open Tabs** view rounds up every open tab across every Chrome window and lays them out in one list. This page shows you where that list lives, how to shape it, and what you can do to a tab from it.

Even tabs that have been put to sleep to save memory show up here, listed by their title and web address with a small *suspended* note.

<Note>
Open Tabs is a live list. As you open, close, and jump between tabs, the view keeps up on its own.
</Note>

## Where it lives

Open Tabs is in both surfaces.

In the card and side panel it is on from the start, as a pinned section above your workspaces. If you would rather have it out of the way, go to **Settings → Open Tabs view → Placement** and switch to **Dedicated tab**, and it becomes its own **Open Tabs** tab instead.

On the dashboard, click **Open Tabs** at the top of the sidebar, where it shows a live count of how many tabs you have. The whole main area becomes the list, with room to breathe.

## Make the list read your way

Two settings shape the list on both surfaces, both under **Settings → Open Tabs view**.

**Group by window** is on by default. The list gets a divider per Chrome window, **This window** and **Window 2** and so on, with the current window also showing which workspace it is using. Turn it off for one flat **All open tabs** list.

**Sort tabs by** offers **Window order (tab strip)** to mirror the order of your real tabs, **Most recently active** to float what you touched last, or **Site name (A–Z)** to group by site.

## What you can do with a tab {#what-you-can-do-with-a-tab}

Click any row and you go straight to that tab, even if it is in another window. Hover a row and a set of quick actions appears:

| Action | What it does |
|--------|--------------|
| **Rename tab** | Type your own name right in the row. Enter saves, Esc cancels. See [why renaming helps](/help/unscrambled-tabs/manage-tabs#rename-any-tab). |
| **Suspend tab** | Puts that tab to sleep and frees its memory. It stays on the tab strip and reloads when you click it. Hidden on the tab you are using and on tabs already asleep. |
| **Save to workspace** | Saves the tab into a workspace so it is safe to close. |
| **Save as resource** | Files the tab into a workspace's saved links instead. |
| **Close tab** | Closes it, right from the list. |

Two small markers help you triage. A green dot means that tab is front-and-centre in its window. A green check means it is already saved in the workspace you are viewing.

Drag any row onto a workspace to file the tab there, which works in the card, the side panel and the dashboard. Hold **⌥ Option** (**Alt** on Windows and Linux) as you let go to copy instead of move, and you can undo any drop for ten seconds afterwards. See [Moving tabs](/help/unscrambled-tabs/moving-tabs).

To take several at once, hover a row and a small square appears where its icon was. Click it to start selecting, then **⌘-click** (**Ctrl-click**) for one more row or **Shift-click** for a run of them. Drag any selected row and the whole selection goes together. **Esc** clears it.

## Whole-window moves {#whole-window-moves}

When grouping by window is on, each window's divider carries two bigger levers.

**Save all** saves every tab in that window into a brand-new workspace. A small dialog suggests a name, and clicking **Create & save** captures the whole window in one go. This is the one to use when the window *is* the project.

**Suspend others** puts every eligible background tab in that window to sleep at once, leaving your current tab awake.

## Close duplicates in one click {#close-duplicates-in-one-click}

Opened the same page three times across two windows? When the list spots duplicates, a **Close duplicates** button appears with the count, in the dashboard panel header and at the top of the side panel's pinned **Open Tabs** section.

One click closes the extras across every window. It always keeps the copy you are using, and it never touches pinned tabs or tabs you have renamed apart on purpose. The full rules are on [Rename & tidy tabs](/help/unscrambled-tabs/manage-tabs#close-duplicate-tabs-in-one-click).

## Your AI knows about these tabs too

The same open tabs feed into the [AI assistant](/help/unscrambled-tabs/ai-and-search), so you can ask the chat something like *"which of my open tabs haven't I saved yet?"* and it knows the answer, because it can see the same list you can.

## A quick note on memory

Listing a sleeping tab does not wake it up. Open Tabs reads the title and address only, so your sleeping tabs stay asleep. For more on keeping memory in check, see [Performance & RAM](/help/unscrambled-tabs/performance-and-ram).
